import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'

const Home = () => {
  return <div></div>
}

ReactDOM.render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
  document.getElementById('root')
)
